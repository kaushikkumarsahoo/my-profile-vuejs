version: '3.4'

services:
  myprofilevuejs:
    image: myprofilevuejs
    environment:
      NODE_ENV: production
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8090:8090

